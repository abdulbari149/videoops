{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "pnpm install",
  "buildCommand": "pnpm exec prisma migrate deploy && pnpm exec prisma generate && pnpm exec next build"
}
