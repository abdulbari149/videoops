# Local Postgres (see docker-compose.yml): docker compose up -d
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/videoops"
AUTH_SECRET=
AUTH_URL=http://localhost:3000
# Optional Google OAuth
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=

# Optional — exposed to the browser (see src/config/env.ts)
# NEXT_PUBLIC_APP_URL=http://localhost:3000
